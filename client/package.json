{
  "name": "razor-outlands-lsp-client",
  "description": "Razor UO Outlands Script LSP client for VS Code",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "vscode": "^1.75.0"
  },
  "scripts": {
    "build": "esbuild src/extension.ts --bundle --platform=node --format=cjs --packages=bundle --external:vscode --outfile=out/extension.js"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@types/vscode": "^1.75.0",
    "@types/node": "^18.19.0"
  }
}
